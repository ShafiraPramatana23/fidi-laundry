package com.fidilaundry.app.utilimport android.os.Environmentobject Constant {    var ROOT_DIR = Environment.getExternalStorageDirectory().path    var PICTURES = "$ROOT_DIR/Pictures"    var CAMERA = "$ROOT_DIR/DCIM/camera"    var STORIES = "$ROOT_DIR/Stories"    const val BaseURL = "https://api-dev.posgo.id/v1/"    val SUCCESSCODE = 200    val NOTFOUND = 404    val SOURCE = "MOBILE"}